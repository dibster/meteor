Router.map(function () {
  this.route("login", {
    path: "/login",
    template: "login"
  });
});
Router.map(function () {
  this.route("register", {
    path: "/register",
    template: "register"
  });
});